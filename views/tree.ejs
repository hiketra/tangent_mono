<% if (items.length) { %>
<ul>
<% } %>

    <% items.forEach(function(item){ %>
    <li>
        <a href="<%= item.id %>"><%= item.message %></a>
        <% if('children' in item) { %>
          <%- partial('treeView', {items: item.children}) %>
        <%}%>
    </li>
    <% }) %>

<% if (items.length) { %>
</ul>
<% } %>
